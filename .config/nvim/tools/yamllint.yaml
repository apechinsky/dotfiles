extends: default

rules:
  document-start: disable

  line-length:
    max: 120
    level: warning

  indentation:
    spaces: consistent
    indent-sequences: true
    check-multi-line-strings: false

  truthy:
    level: warning
