return {
    'nvim-tree/nvim-tree.lua',

    opts = {
        sync_root_with_cwd = false,

        view = {
            width = 40,
        },

        filters = {
          dotfiles = false,
        },
    }
}
